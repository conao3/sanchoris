[[pre-start]]
copy_ignored = "wt step copy-ignored"

[[pre-start]]
env = "node scripts/worktrunk-env-sync.mjs"

[[pre-start]]
setup = "direnv allow && nix develop --command pnpm install"

[aliases]
dev = "pnpm dev"
urls = "portless list"

[step.copy-ignored]
exclude = [
  "apps/frontend/dist/",
]
