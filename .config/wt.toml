[pre-start]
setup = "wt step copy-ignored && direnv allow && nix develop --command pnpm install"

[aliases]
dev = "pnpm dev"
urls = "portless list"

[step.copy-ignored]
exclude = [
  "apps/frontend/dist/",
]
